# M6: 轮次调度与完整任务编排 - 任务清单

> 目标：实现按轮次执行巡逻，一轮结束后返回待命点并定时启动下一轮。

- [x] **M6.1** 实现轮次结束逻辑
  - 当 `current_wp_index` 等于巡逻点总数时，`PatrolRound` 返回 SUCCESS
- [x] **M6.2** 实现 `ReturnToDock` 动作
  - 导航到 `dock_prep_pose`，不进行视觉对接（仅待命）
- [x] **M6.3** 实现定时等待节点
  - 使用 `Delay` 节点等待 `round_interval` 秒（可参数化）
- [x] **M6.4** 组合主行为树循环
  - 顶层 ReactiveSequence：电量检查 → 巡逻 → 返回待命点 → 等待 → 重新开始巡逻
- [x] **M6.5** 验证断点续巡逻
  - 充电恢复后应从之前中断的点继续，一轮内不重置索引
- [x] **M6.6** 验证轮次重置
  - 一轮结束后，回到待命点，等待固定间隔后，索引清零，重新开始第一轮